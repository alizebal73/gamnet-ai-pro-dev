# Pinned dependencies (tested on Python 3.13).
# Upgrade intentionally: bump + run pytest on a fresh venv.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
bcrypt==5.0.0
httpx==0.28.1
pytest==9.0.3
pytest-asyncio==1.4.0
